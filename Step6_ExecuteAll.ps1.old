# =================================================================
# Fichier     : Step6_ExecuteAll.ps1
# Role        : Centre commercial principal pour l'exécution de toutes les actions
# Magasins    : - Magasin des fonctions (copie des fonctions nécessaires)
#               - Magasin des exécutions (séquence d'actions)
# =================================================================

# Vérification des droits d'administrateur
$isAdmin = ([Security.Principal.WindowsPrincipal] [Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
if (-not $isAdmin) {
    Write-Host "⚠️ Ce script nécessite des droits d'administrateur." -ForegroundColor Yellow
    Write-Host "Relancement du script avec les droits d'administrateur..." -ForegroundColor Cyan
    
    try {
        Start-Process pwsh.exe -ArgumentList "-NoProfile -ExecutionPolicy Bypass -File `"$PSCommandPath`"" -Verb RunAs -Wait
        exit
    }
    catch {
        Write-Host "❌ Impossible d'obtenir les droits d'administrateur: $_" -ForegroundColor Red
        exit 1
    }
}

# ===== Inclusions des modules =====
# Inclusion de notre module de gestion du registre
$registryManagerPath = Join-Path $PSScriptRoot "Step7_RegistryManager.ps1"
if (Test-Path $registryManagerPath) {
    . $registryManagerPath
} else {
    Write-Host "❌ Module de gestion du registre non trouvé: $registryManagerPath" -ForegroundColor Red
}

# Inclusion du module de gestion des adresses MAC
$macAddressPath = Join-Path $PSScriptRoot "Step4_MacAddress.ps1"
if (Test-Path $macAddressPath) {
    . $macAddressPath
    Write-Host "✓ Module de gestion des adresses MAC chargé" -ForegroundColor Green
} else {
    Write-Host "❌ Module de gestion des adresses MAC non trouvé: $macAddressPath" -ForegroundColor Red
}

# Inclusion du module de gestion des fichiers
$fileManagerPath = Join-Path $PSScriptRoot "Step5_FileManager.ps1"
if (Test-Path $fileManagerPath) {
    . $fileManagerPath
    Write-Host "✓ Module de gestion des fichiers chargé" -ForegroundColor Green
} else {
    Write-Host "❌ Module de gestion des fichiers non trouvé: $fileManagerPath" -ForegroundColor Red
}

# ===== Magasin des fonctions importées =====

# ----- Fonctions MAC -----
# Les fonctions MAC sont maintenant importées depuis Step4_MacAddress.ps1

# Fonction réutilisable pour afficher le résumé des actions
function Show-ActionSummary {
    param (
        [Parameter(Mandatory=$true)]
        [bool]$MacSuccess,
        [Parameter(Mandatory=$true)]
        [bool]$StorageSuccess,
        [Parameter(Mandatory=$true)]
        [bool]$MachineGuidSuccess,
        [Parameter(Mandatory=$false)]
        [string]$StorageMessage = ""
    )
    
    # Créer la fenêtre de résumé avec les boutons - Augmentation de la taille
    $summaryForm = New-Object System.Windows.Forms.Form
    $summaryForm.Text = "Résumé"
    $summaryForm.Size = New-Object System.Drawing.Size(450, 350) # Taille augmentée
    $summaryForm.StartPosition = "CenterScreen"
    $summaryForm.BackColor = [System.Drawing.Color]::FromArgb(30,30,30)
    $summaryForm.ForeColor = [System.Drawing.Color]::White
    $summaryForm.FormBorderStyle = [System.Windows.Forms.FormBorderStyle]::FixedSingle
    $summaryForm.MaximizeBox = $false
    $summaryForm.TopMost = $true
    
    # Label de résumé avec meilleure mise en forme et gestion du message Storage
    $storageResultMessage = if($StorageSuccess) {
        if($StorageMessage -eq "Le fichier storage.json n'existe pas.") {
            "Fichier déjà supprimé"
        } else {
            "Fichier supprimé avec succès"
        }
    } else {
        "Échec - $StorageMessage"
    }
    
    $summaryLabel = New-Object System.Windows.Forms.Label
    $summaryLabel.Text = @"
Résumé des actions :

$(if($MacSuccess){"✅"}else{"❌"}) MAC Address: $(if($MacSuccess){"Modifiée avec succès"}else{"Échec"})
$(if($MachineGuidSuccess){"✅"}else{"❌"}) MachineGuid: $(if($MachineGuidSuccess){"Réinitialisé avec succès"}else{"Échec"})
$(if($StorageSuccess){"✅"}else{"❌"}) Storage: $storageResultMessage

Veuillez procéder à votre nouvelle inscription sur cursor.com
"@
    $summaryLabel.Font = New-Object System.Drawing.Font("Segoe UI", 10)
    $summaryLabel.ForeColor = [System.Drawing.Color]::White
    $summaryLabel.TextAlign = [System.Drawing.ContentAlignment]::MiddleCenter
    $summaryLabel.Size = New-Object System.Drawing.Size(430, 160) # Taille augmentée
    $summaryLabel.Location = New-Object System.Drawing.Point(10, 20)
    $summaryForm.Controls.Add($summaryLabel)
    
    # Bouton Cursor
    $btnCursor = New-Object System.Windows.Forms.Button
    $btnCursor.Text = "Aller sur cursor.com"
    $btnCursor.Font = New-Object System.Drawing.Font("Segoe UI", 10)
    $btnCursor.Size = New-Object System.Drawing.Size(200, 35)
    $btnCursor.Location = New-Object System.Drawing.Point(125, 190) # Position ajustée
    $btnCursor.BackColor = [System.Drawing.Color]::FromArgb(255,140,0)
    $btnCursor.ForeColor = [System.Drawing.Color]::White
    $btnCursor.FlatStyle = [System.Windows.Forms.FlatStyle]::Flat
    $btnCursor.FlatAppearance.BorderSize = 1
    $btnCursor.FlatAppearance.BorderColor = [System.Drawing.Color]::FromArgb(255,140,0)
    $btnCursor.Cursor = [System.Windows.Forms.Cursors]::Hand
    $btnCursor.Add_Click({
        Start-Process "https://cursor.com"
    })
    $summaryForm.Controls.Add($btnCursor)
    
    # Bouton Emails Temporaires
    $btnExtension = New-Object System.Windows.Forms.Button
    $btnExtension.Text = "Emails Temporaires"
    $btnExtension.Font = New-Object System.Drawing.Font("Segoe UI", 10)
    $btnExtension.Size = New-Object System.Drawing.Size(200, 35)
    $btnExtension.Location = New-Object System.Drawing.Point(125, 240) # Position ajustée
    $btnExtension.BackColor = [System.Drawing.Color]::FromArgb(255,140,0)
    $btnExtension.ForeColor = [System.Drawing.Color]::White
    $btnExtension.FlatStyle = [System.Windows.Forms.FlatStyle]::Flat
    $btnExtension.FlatAppearance.BorderSize = 1
    $btnExtension.FlatAppearance.BorderColor = [System.Drawing.Color]::FromArgb(255,140,0)
    $btnExtension.Cursor = [System.Windows.Forms.Cursors]::Hand
    $btnExtension.Add_Click({
        Start-Process "https://chromewebstore.google.com/detail/temporary-email-emailonde/mkpcaklladfpajiaikehdinfaabmnajh"
    })
    $summaryForm.Controls.Add($btnExtension)
    
    # Afficher la fenêtre de résumé
    $summaryForm.ShowDialog()
}

# ----- Fonctions d'exécution -----
function Start-AllActions {
    param (
        [switch]$SkipMacChange,
        [switch]$SkipGuidChange,
        [switch]$SkipFileCleanup
    )
    
    Write-Host "`n🏪 Démarrage de toutes les actions..." -ForegroundColor Cyan
    $results = @{
        MAC = $false
        Storage = $false
        MachineGuid = $false
    }
    
    try {
        # Étape 1 : Modification de l'adresse MAC
        if (-not $SkipMacChange) {
        Write-Host "`n=== Étape 1: Modification de l'adresse MAC ===" -ForegroundColor Yellow
        $adapter = Get-NetworkAdapters | Select-Object -First 1
        if ($adapter) {
                # Améliorer l'affichage pour les cartes Realtek (Gigabyte)
                $adapterDesc = $adapter.InterfaceDescription
                if ($adapterDesc -match "Realtek PCIe GbE Family Controller") {
                    Write-Host "  ✓ Adaptateur trouvé: Gigabyte Ethernet [$($adapter.Name)]" -ForegroundColor Green
                } else {
                    Write-Host "  ✓ Adaptateur trouvé: $adapterDesc [$($adapter.Name)]" -ForegroundColor Green
                }
                
            $newMac = New-MacAddress
            if ($newMac) {
                    # Appel avec les paramètres corrects pour garantir la désactivation/réactivation
                    $macResult = Set-MacAddress -AdapterName $adapter.Name -MacAddress $newMac
                    $results.MAC = ($macResult -eq $true)
                    
                if ($results.MAC) {
                        Write-Host "  ✓ Adresse MAC modifiée avec succès: $newMac" -ForegroundColor Green
                    Write-Host "  ⏳ Attente de la reconnexion réseau (10 secondes)..." -ForegroundColor Yellow
                    Start-Sleep -Seconds 10
                    } else {
                        Write-Host "  ❌ Échec de la modification de l'adresse MAC" -ForegroundColor Red
                    }
                }
            }
        } else {
            Write-Host "`n=== Étape 1: Modification de l'adresse MAC [IGNORÉE] ===" -ForegroundColor Gray
        }

        # Étape 2 : Modification du MachineGuid
        if (-not $SkipGuidChange) {
            Write-Host "`n=== Étape 2: Modification du MachineGuid ===" -ForegroundColor Yellow
            $guidResult = Reset-MachineGuid
            $results.MachineGuid = $guidResult.Success
            if ($results.MachineGuid) {
                Write-Host "  ✓ MachineGuid modifié avec succès" -ForegroundColor Green
                Write-Host "    Ancien: $($guidResult.OldValue)" -ForegroundColor Gray
                Write-Host "    Nouveau: $($guidResult.NewValue)" -ForegroundColor Green
            } else {
                Write-Host "  ❌ Échec de la modification du MachineGuid: $($guidResult.Message)" -ForegroundColor Red
            }
        } else {
            Write-Host "`n=== Étape 2: Modification du MachineGuid [IGNORÉE] ===" -ForegroundColor Gray
        }

        # Étape 3 : Suppression du fichier storage.json
        if (-not $SkipFileCleanup) {
            Write-Host "`n=== Étape 3: Suppression du fichier storage.json ===" -ForegroundColor Yellow
        $storageResult = Remove-CursorStorage
        $results.Storage = $storageResult.Success
            if ($results.Storage) {
                if ($storageResult.Message -match "n'existe pas") {
                    Write-Host "  ✓ Fichier storage.json déjà supprimé" -ForegroundColor Green
                } else {
                    Write-Host "  ✓ Fichier storage.json supprimé avec succès" -ForegroundColor Green
                }
            } else {
                Write-Host "  ❌ Échec de la suppression: $($storageResult.Message)" -ForegroundColor Red
            }
        } else {
            Write-Host "`n=== Étape 3: Suppression du fichier storage.json [IGNORÉE] ===" -ForegroundColor Gray
        }

        # Résumé des actions
        Write-Host "`n=== Résumé des actions ===" -ForegroundColor Magenta
        Write-Host "  MAC Address: $(if ($results.MAC) { "✓ Succès" } else { "❌ Échec" })" -ForegroundColor $(if ($results.MAC) { "Green" } else { "Red" })
        Write-Host "  MachineGuid: $(if ($results.MachineGuid) { "✓ Succès" } else { "❌ Échec" })" -ForegroundColor $(if ($results.MachineGuid) { "Green" } else { "Red" })
        Write-Host "  Storage: $(if ($results.Storage) { "✓ Succès" } else { "❌ Échec" })" -ForegroundColor $(if ($results.Storage) { "Green" } else { "Red" })

        return $results
    } catch {
        Write-Host "❌ Erreur lors de l'exécution des actions: $_" -ForegroundColor Red
        return $results
    }
} 

function Execute-AllActions {
    param (
        [Parameter(Mandatory=$true)]
        [string]$MacAddress,
        [Parameter(Mandatory=$false)]
        [string]$InterfaceName = "",
        [Parameter(Mandatory=$true)]
        [bool]$ShouldResetMachineGuid,
        [Parameter(Mandatory=$true)]
        [bool]$ShouldDeleteStorageFile,
        [Parameter(Mandatory=$false)]
        [System.Windows.Forms.ProgressBar]$ProgressControl = $null,
        [Parameter(Mandatory=$false)]
        [System.Windows.Forms.Label]$StatusLabel = $null
    )
    
    # Initialisation du tableau de résultats
        $results = @{
            MAC = $false
        MachineGuid = $false
            Storage = $false
    }

    # Tenir un journal des messages (pour le résumé)
    $storageMessage = ""
    
    # Fonction pour mettre à jour la progression - Avec plus d'étapes intermédiaires
    function Update-Progress {
        param (
            [int]$Value,
            [string]$StatusText
        )
        
        if ($ProgressControl) {
            # Transition progressive de la barre
            $currentValue = $ProgressControl.Value
            $step = if ($Value -gt $currentValue) { 5 } else { -5 }
            
            while ($currentValue -ne $Value) {
                $currentValue += $step
                if (($step -gt 0 -and $currentValue -gt $Value) -or 
                    ($step -lt 0 -and $currentValue -lt $Value)) {
                    $currentValue = $Value
                }
                $ProgressControl.Value = $currentValue
                Start-Sleep -Milliseconds 30
                [System.Windows.Forms.Application]::DoEvents()
            }
        }
        
        if ($StatusLabel -and $StatusText) {
            $StatusLabel.Text = $StatusText
        }
        
        # Permettre à l'interface de se mettre à jour
                [System.Windows.Forms.Application]::DoEvents()
            }
            
    # 1. Modifier MAC Address si nécessaire
    if ($MacAddress -ne "") {
        try {
            Write-Host "Modification de l'adresse MAC..."
            Update-Progress -Value 10 -StatusText "Préparation de la modification MAC..."
            
            # Si le nom d'interface n'est pas spécifié, nous utilisons la première interface active
            if ($InterfaceName -eq "") {
                Update-Progress -Value 15 -StatusText "Recherche d'interfaces réseau..."
                $adapter = Get-NetAdapter | Where-Object { $_.Status -eq "Up" } | Select-Object -First 1
                if ($adapter) {
                    $InterfaceName = $adapter.Name
                    Update-Progress -Value 20 -StatusText "Interface trouvée: $InterfaceName"
                }
                else {
                    Write-Host "Aucune interface réseau active trouvée." -ForegroundColor Red
                    Update-Progress -Value 100 -StatusText "Erreur: Aucune interface réseau active trouvée"
                    return @{
                        Results = $results
                        StorageMessage = $storageMessage
                    }
                }
            }
            
            # Appeler la fonction pour changer l'adresse MAC
            Update-Progress -Value 25 -StatusText "Modification de l'adresse MAC en cours..."
            
            # Vérification si nous sommes en mode test
            if ($env:TEST_MODE -eq $true) {
                # Mode test - simulation
                Write-Host "SIMULATION: Modification de l'adresse MAC de $InterfaceName à $MacAddress"
                $results.MAC = $true
                            } else {
                # Modification réelle de l'adresse MAC avec les paramètres corrects
                Write-Host "Appel de Set-MacAddress avec AdapterName=$InterfaceName et MacAddress=$MacAddress"
                $macResult = Set-MacAddress -AdapterName $InterfaceName -MacAddress $MacAddress
                $results.MAC = ($macResult -eq $true)
            }
            
            if ($results.MAC) {
                Write-Host "Adresse MAC modifiée avec succès." -ForegroundColor Green
                Update-Progress -Value 35 -StatusText "Adresse MAC modifiée avec succès"
                
                # La fonction Set-MacAddress désactive et réactive déjà la carte réseau
                # Il n'est donc pas nécessaire d'attendre dans une boucle, simplement attendre un délai fixe
                Write-Host "Attente après la modification de l'adresse MAC (5 secondes)..." -ForegroundColor Yellow
                Update-Progress -Value 40 -StatusText "Attente après modification..."
                Start-Sleep -Seconds 5
                Update-Progress -Value 50 -StatusText "Modification MAC terminée"
                    } else {
                Write-Host "Échec de la modification de l'adresse MAC." -ForegroundColor Red
                Update-Progress -Value 50 -StatusText "Échec de la modification MAC"
                    }
                }
                catch {
            Write-Host "Erreur lors de la modification de l'adresse MAC: $_" -ForegroundColor Red
            Update-Progress -Value 50 -StatusText "Erreur: Modification MAC Address"
            $results.MAC = $false
        }
    }

    # 2. Réinitialiser MachineGuid si nécessaire
    if ($ShouldResetMachineGuid) {
        try {
            Write-Host "Réinitialisation du MachineGuid..."
            Update-Progress -Value 55 -StatusText "Réinitialisation du MachineGuid..."
            
            # En mode test, on simule la réinitialisation
            if ($env:TEST_MODE -eq $true) {
                Write-Host "SIMULATION: Réinitialisation du MachineGuid réussie"
                $results.MachineGuid = $true
            } else {
                $guidResult = Reset-MachineGuid
                $results.MachineGuid = $guidResult.Success
            }
            
            if ($results.MachineGuid) {
                Write-Host "MachineGuid réinitialisé avec succès." -ForegroundColor Green
                Update-Progress -Value 70 -StatusText "MachineGuid réinitialisé"
            } else {
                Write-Host "Échec de la réinitialisation du MachineGuid." -ForegroundColor Red
                Update-Progress -Value 70 -StatusText "Échec réinitialisation MachineGuid"
            }
        }
        catch {
            Write-Host "Erreur lors de la réinitialisation du MachineGuid: $_" -ForegroundColor Red
            Update-Progress -Value 70 -StatusText "Erreur: Réinitialisation MachineGuid"
            $results.MachineGuid = $false
        }
    } else {
        Update-Progress -Value 70 -StatusText "Réinitialisation MachineGuid ignorée"
    }

    # 3. Supprimer le fichier storage.json si nécessaire
    if ($ShouldDeleteStorageFile) {
        try {
            Write-Host "Suppression du fichier storage.json..."
            Update-Progress -Value 75 -StatusText "Vérification du fichier storage.json..."
            
            $filePath = "$env:APPDATA\Cursor\storage.json"
            
            # En mode test, on simule la suppression
            if ($env:TEST_MODE -eq $true) {
                Write-Host "SIMULATION: Suppression du fichier storage.json"
                $results.Storage = $true
                $storageMessage = "Le fichier storage.json a été simulé comme supprimé."
            } else {
                if (Test-Path $filePath) {
                    Update-Progress -Value 80 -StatusText "Suppression du fichier storage.json..."
                    Remove-Item -Path $filePath -Force
                    $results.Storage = $true
                    Write-Host "Fichier storage.json supprimé avec succès." -ForegroundColor Green
                    $storageMessage = ""
                }
                else {
                    $storageMessage = "Le fichier storage.json n'existe pas."
                    Write-Host $storageMessage -ForegroundColor Yellow
                    $results.Storage = $true  # Considéré comme succès car l'objectif est que le fichier n'existe pas
                }
            }
            
            Update-Progress -Value 90 -StatusText "Gestion du fichier storage.json terminée"
        }
        catch {
            $storageMessage = "Erreur: $_"
            Write-Host "Erreur lors de la suppression du fichier storage.json: $_" -ForegroundColor Red
            Update-Progress -Value 90 -StatusText "Erreur: Suppression storage.json"
            $results.Storage = $false
        }
    } else {
        Update-Progress -Value 90 -StatusText "Suppression storage.json ignorée"
    }

    # 4. Mise à jour finale
    Update-Progress -Value 100 -StatusText "Terminé"

    # 5. Retourner les résultats
    return @{
        Results = $results
        StorageMessage = $storageMessage
    }
}

# Interface pour le bouton "Executer toutes les actions"
function Initialize-ExecuteAllButton {
    param (
        [System.Windows.Forms.Form]$Form
    )
    
    # Création du bouton
    $button = New-Object System.Windows.Forms.Button
    $button.Text = "Exécuter toutes les actions"
    $button.Location = New-Object System.Drawing.Point(15, 15)
    $button.Width = 200
    $button.Height = 40
    $button.Font = New-Object System.Drawing.Font("Segoe UI", 9, [System.Drawing.FontStyle]::Regular)
    
    # Création de la barre de progression
    $progressBar = New-Object System.Windows.Forms.ProgressBar
    $progressBar.Location = New-Object System.Drawing.Point(15, 65)
    $progressBar.Size = New-Object System.Drawing.Size(200, 20)
    $progressBar.Style = 'Continuous'
    $progressBar.Value = 0
    $Form.Controls.Add($progressBar)
    
    # Création du label de statut
    $statusLabel = New-Object System.Windows.Forms.Label
    $statusLabel.Location = New-Object System.Drawing.Point(15, 95)
    $statusLabel.Size = New-Object System.Drawing.Size(200, 40)
    $statusLabel.Font = New-Object System.Drawing.Font("Segoe UI", 9, [System.Drawing.FontStyle]::Regular)
    $statusLabel.Text = "Prêt"
    $Form.Controls.Add($statusLabel)
    
    # Gestionnaire d'événements du bouton
    $button.Add_Click({
        # Lire les valeurs des champs
        $newMacAddress = $textBoxMac.Text.Trim()
        $resetMachineGuid = $checkboxGuid.Checked
        $deleteStorage = $checkboxStorage.Checked
        
        # Interface sélectionnée
        $selectedAdapter = $null
        if ($comboboxAdapters.SelectedItem) {
            $selectedAdapter = $comboboxAdapters.SelectedItem.ToString()
        }
        
        # Vérifier qu'au moins une action est sélectionnée
        if ($newMacAddress -eq "" -and -not $resetMachineGuid -and -not $deleteStorage) {
            [System.Windows.Forms.MessageBox]::Show("Veuillez sélectionner au moins une action à effectuer.", "Erreur", [System.Windows.Forms.MessageBoxButtons]::OK, [System.Windows.Forms.MessageBoxIcon]::Warning)
            return
        }
        
        # Confirmer avant exécution
        $confirmResult = [System.Windows.Forms.MessageBox]::Show(
            "Voulez-vous vraiment exécuter ces actions ?`n`n" + 
            $(if($newMacAddress -ne ""){"- Modifier l'adresse MAC à $newMacAddress`n"} else {""}) +
            $(if($resetMachineGuid){"- Réinitialiser le MachineGuid`n"} else {""}) +
            $(if($deleteStorage){"- Supprimer le fichier storage.json`n"} else {""}),
            "Confirmation",
            [System.Windows.Forms.MessageBoxButtons]::YesNo,
            [System.Windows.Forms.MessageBoxIcon]::Question
        )
        
        if ($confirmResult -eq [System.Windows.Forms.DialogResult]::Yes) {
            # Exécuter les actions sélectionnées
            $progressBar.Value = 0
            $statusLabel.Text = "Exécution des actions..."
            
            $result = Execute-AllActions -MacAddress $newMacAddress -InterfaceName $selectedAdapter -ShouldResetMachineGuid $resetMachineGuid -ShouldDeleteStorageFile $deleteStorage
            
            # Afficher le résumé des actions
            Show-ActionSummary -MacSuccess $result.Results.MAC -StorageSuccess $result.Results.Storage -MachineGuidSuccess $result.Results.MachineGuid -StorageMessage $result.StorageMessage
        }
    })
    
    # Ajustement de la taille du formulaire
    $Form.ClientSize = New-Object System.Drawing.Size(230, 150)
    $Form.FormBorderStyle = 'FixedDialog'
    $Form.MaximizeBox = $false
    $Form.StartPosition = 'CenterScreen'
    $Form.Text = "Exécution des actions"
    
    return $button
}

# Si le script est exécuté directement, lancer les actions
if ($MyInvocation.InvocationName -ne '.') {
    # Vérifier si on veut l'interface graphique
    if ($args -contains "-GUI") {
        Add-Type -AssemblyName System.Windows.Forms
        $form = New-Object System.Windows.Forms.Form
        $button = Initialize-ExecuteAllButton -Form $form
        $form.Controls.Add($button)
        $form.ShowDialog()
    } else {
        Start-AllActions
    }
}





